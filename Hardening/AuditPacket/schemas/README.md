# Schemas

The immutable packet generator copies the exact `Hardening/schemas` directory here at
generation time. This source directory intentionally contains only this pointer to avoid
two independently editable schema copies.
