# Security test inventory

The generated packet includes the repository's V0.3, V0.5, V0.6, V0.7, and V0.8 security
test plans plus the relevant `VelaTests`, `VelaHelperTests`, `VelaPrivilegedIntegrationTests`,
and `VelaIPC/Tests` sources. Test presence is not a pass; the final packet must also include
the exact signed build's test manifests and cleanup evidence.
