import SwiftUI

enum PageRecoveryActionMetrics {
    /// Minimum width for the label content. Native button chrome adds the
    /// platform-appropriate horizontal padding around this value.
    static let compactContentMinimumWidth: CGFloat = 96

    /// Keeps prominent buttons and menu labels on the same 30–32 pt control grid.
    static let contentHeight: CGFloat = 22
}
