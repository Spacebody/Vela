# 场景与自动化


场景可以选择配置、网络后端、Mihomo 模式、代理节点和场景配置层。

触发条件可以使用网络接口类型、可选的当前 Wi-Fi 名称、本地时间、供电状态，以及网络是否
受限或计费。

只有在添加 Wi-Fi 名称触发条件时，Vela 才会请求定位权限。Vela 只读取当前连接的 Wi-Fi
名称，不扫描附近网络，也不读取 BSSID。准确名称保存在钥匙串中。

防抖、稳定窗口、冷却时间和手动锁定可以避免频繁切换。
