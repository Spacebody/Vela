# 系统代理与 TUN


## 系统代理

系统代理会设置 macOS 的 HTTP、HTTPS 与 SOCKS 代理。它更轻量，但不遵循系统代理设置的
应用可能仍然直连。

## TUN

TUN 会让更多设备流量进入 Mihomo，并且需要 Vela 已签名的特权组件。

Vela 将系统代理和 TUN 视为互斥模式。切换过程是事务化的：如果目标模式失败，Vela 会尝试
恢复之前的可用模式。

建议先使用系统代理。需要更完整的流量覆盖或 UDP 时，再使用 TUN。
