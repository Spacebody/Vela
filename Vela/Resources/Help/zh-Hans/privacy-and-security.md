# 隐私与安全


Vela 的设计原则是尽量让运行数据留在本机。

- 订阅凭据和已配置的 Wi-Fi 名称保存在钥匙串中。
- 连接快照默认只保存在内存。
- Vela 不包含远程使用统计。
- Vela 不会自动上传崩溃报告。
- 帮助内容随应用离线提供。
- 只有在你明确操作后，Vela 才会生成支持包。

Vela 可能访问用户配置的订阅/Provider 服务器、签名应用更新 Feed 和签名内核目录。

报告安全漏洞时，请使用 Vela 安全政策中的私密渠道。不要公开发布凭据或私有配置。
