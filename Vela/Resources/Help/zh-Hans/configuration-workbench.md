# 配置工作台


配置工作台会根据上游配置、全局层、配置层、场景层、Vela 强制运行参数，以及 TUN 模式下的
特权净化报告，编译最终 Mihomo 配置。

原始配置永远不会被修改。

通用配置层不能修改 Vela 管理的 Controller 密钥、监听暴露、管理端口或 TUN 安全字段。

可在规则工作台中前置或追加 Vela 自己管理的规则。每次保存都会编译、执行 `mihomo -t`
校验，并通过支持回滚的事务应用。
